Hi Koalas, Welcome to github. Please write a sentence after your name introducing yourself in this file and git push.

-------AMAR--------
My name is Amar and I enjoy building and designing.
I'm in 7th grade and I go to Sterling School CTC.


--------AVINASH--------
I'm Avinash and I love building and doing math.
I go to Sterling School CTC and I'm in 7th grade.




--------AKSHAJ----------

My name is akshaj and this is my 4th github commit! check this out:

package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;

@TeleOp
public class DriveTrain extends LinearOpMode {

    public void runOpMode() {

        DcMotor fl = hardwareMap.get(DcMotor.class, "front_left_drive");
        DcMotor bl = hardwareMap.get(DcMotor.class, "back_left_drive");
        DcMotor fr = hardwareMap.get(DcMotor.class, "front_right_drive");
        DcMotor br = hardwareMap.get(DcMotor.class, "back_right_drive");

        DcMotor intakeMotor = hardwareMap.get(DcMotor.class, "intake_motor");
        DcMotor shooterMotor = hardwareMap.get(DcMotor.class, "shooter_motor");

        fl.setDirection(DcMotor.Direction.REVERSE);
        bl.setDirection(DcMotor.Direction.REVERSE);

        double multiplier = 1.0;

        waitForStart();

        while (opModeIsActive()) {

            // Drive
            double power = -gamepad1.right_stick_y;
            double powerst = gamepad1.right_stick_x;
            double turn = gamepad1.left_stick_x;


            double flPower = power + powerst + turn;
            double blPower = power - powerst + turn;
            double frPower = power - powerst - turn;
            double brPower = power + powerst - turn;


            fl.setPower(flPower);
            bl.setPower(blPower);
            fr.setPower(frPower);
            br.setPower(brPower);


            // Shooter and intake
            shooterMotor.setPower(gamepad1.right_trigger);
            intakeMotor.setPower(gamepad1.left_trigger);


            telemetry.addData("Right Trigger", gamepad1.right_trigger);
            telemetry.addData("Left Trigger", gamepad1.left_trigger);
            telemetry.update();
        }
    }
}



----------OM----------

My name is Om. Did you know that? Guess which school I go to... Did you know that I am in 7th grade?



--------SHIVANI---------


My name is Shivani and this is my first year of FTC.
Algebra 1 quiz is Next friday for 7th graders.
